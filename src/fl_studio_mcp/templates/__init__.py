"""Bundled FL Studio project templates (data files, not code).

``empty.flp`` is a copy of FL Studio's own *Empty* project template, used by
Route A (:mod:`fl_studio_mcp.routes.pyflp_route`) to mint new ``.flp`` files
offline so they open cleanly in FL.
"""
