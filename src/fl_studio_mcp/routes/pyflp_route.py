"""Route A — PyFLP: read/write FL Studio ``.flp`` project files offline (no FL running).

Best for generating/editing whole projects deterministically. See docs/ARCHITECTURE.md.
Reference: https://github.com/demberto/PyFLP  (``pip install pyflp``)

Scaffold — real tools (``flp_create``, ``flp_add_channel``, ``flp_add_pattern``,
``flp_set_tempo``, ``flp_write``) land per BACKLOG.md.
"""
# from pyflp import parse, save   # enable once implemented


def status() -> str:
    return "Route A (PyFLP, offline .flp read/write): SCAFFOLD — not yet implemented"
