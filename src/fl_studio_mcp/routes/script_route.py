"""Route C — Piano Roll / Edison scripts: one-off Python transforms invoked inside FL.

``flpianoroll`` (notes / markers) and Edison ``enveditor`` (audio) scripts run once from
FL's Scripts menus. Useful as generators the server can drop into a project.
See docs/ARCHITECTURE.md.

Scaffold — the server will ship a library of these scripts per BACKLOG.md.
"""


def status() -> str:
    return "Route C (Piano Roll / Edison one-off scripts): SCAFFOLD — not yet implemented"
